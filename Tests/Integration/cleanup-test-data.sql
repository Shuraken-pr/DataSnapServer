-- =============================================================================
-- Скрипт для ручной очистки тестовых данных
-- =============================================================================
-- Использование:
--   docker exec -it audit-test-db psql -U test_user -d audit_test -f /cleanup-test-data.sql
--
-- Или подключитесь к БД и выполните:
--   SELECT cleanup_test_data();
-- =============================================================================

-- Очистка всех тестовых данных
SELECT cleanup_test_data();

-- Проверка результата
SELECT * FROM v_test_stats;

-- Сообщение
DO $$
BEGIN
    RAISE NOTICE '========================================';
    RAISE NOTICE 'Тестовые данные успешно очищены!';
    RAISE NOTICE '========================================';
END $$;
